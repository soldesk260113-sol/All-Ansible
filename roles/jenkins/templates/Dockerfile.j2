FROM jenkins/jenkins:lts

USER root

# Install Ansible and dependencies
RUN apt-get update && \
    apt-get install -y ansible sshpass python3-venv && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

# Return to root (or jenkins if preferred, but current setup uses root)
USER root
